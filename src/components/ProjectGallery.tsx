import {FileText,Image as ImageIcon} from 'lucide-react'
import {Project} from '../types'
export default function ProjectGallery({project}:{project:Project}){const galleryAssets=project.assets.filter(asset=>asset.isSelectedForGallery);if(!galleryAssets.length)return null;return <section className="project-gallery"><div className="project-gallery-heading"><div><p className="eyebrow">Project gallery</p><h2 className="section-title mt-2">Drawings and imagery</h2></div><span>{galleryAssets.length} assets</span></div><div className="gallery-grid">{galleryAssets.map((asset,index)=><article key={asset.id} className={index===0?'featured':''}>{asset.url?<img src={asset.url||project.coverImage} alt={asset.title}/>:<div className="asset-placeholder">{['plan','section','diagram'].includes(asset.type)?<ImageIcon/>:<FileText/>}</div>}<div><span>{asset.uploadedCategory}</span><strong>{asset.title}</strong></div></article>)}</div></section>}
