import {ReactNode} from 'react'
import {X} from 'lucide-react'
export function Button({children,variant='dark',className='',...props}:React.ButtonHTMLAttributes<HTMLButtonElement>&{variant?:'dark'|'light'|'ghost'|'acid'}){return <button className={`btn btn-${variant} ${className}`} {...props}>{children}</button>}
export function Badge({children,tone='neutral'}:{children:ReactNode;tone?:'neutral'|'green'|'dark'}){return <span className={`badge badge-${tone}`}>{children}</span>}
export function Modal({open,onClose,title,children}:{open:boolean;onClose:()=>void;title:string;children:ReactNode}){if(!open)return null;return <div className="modal-backdrop" onMouseDown={onClose}><div className="modal" onMouseDown={e=>e.stopPropagation()}><div className="flex items-center justify-between mb-6"><h2 className="text-2xl font-semibold">{title}</h2><button onClick={onClose} aria-label="Close"><X size={20}/></button></div>{children}</div></div>}
export function EmptyState({title,body,action}:{title:string;body:string;action?:ReactNode}){return <div className="rounded-3xl border border-dashed border-black/20 p-12 text-center bg-white"><div className="mx-auto mb-5 h-12 w-12 rounded-full bg-acid grid place-items-center text-xl">✦</div><h3 className="text-xl font-semibold">{title}</h3><p className="mt-2 text-black/55 max-w-md mx-auto">{body}</p>{action&&<div className="mt-5">{action}</div>}</div>}
