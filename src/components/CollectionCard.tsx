import {ArrowRight,Shapes} from 'lucide-react'
import {Link} from 'react-router-dom'
import {Collection,Project} from '../types'
export default function CollectionCard({collection,projects}:{collection:Collection;projects:Project[]}){const items=collection.projectIds.map(id=>projects.find(p=>p.id===id)).filter(Boolean) as Project[];return <Link to={`/collections/${collection.id}`} className="group rounded-3xl bg-white border border-black/10 p-4 block hover:shadow-soft transition"><div className="grid grid-cols-2 h-40 rounded-2xl overflow-hidden bg-black/5">{items.slice(0,2).map(p=><img key={p.id} className="w-full h-full object-cover" src={p.coverImage}/>)}{items.length===0&&<div className="col-span-2 grid place-items-center text-black/25"><Shapes size={30}/></div>}</div><div className="p-2 pt-5 flex justify-between"><div><h3 className="text-lg font-semibold">{collection.name}</h3><p className="text-sm text-black/45 mt-1">{items.length} {items.length===1?'project':'projects'}</p></div><ArrowRight className="group-hover:translate-x-1 transition" size={19}/></div></Link>}
