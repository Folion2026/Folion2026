import {Archive,Eye,FolderPlus,X} from 'lucide-react'
import {useState} from 'react'
import {useStore} from '../store'
import {Button,Modal} from './ui'
export default function BottomSelectionBar(){const {selected,collections,clearSelected,archiveSelected,setSelectedVisibility,addSelectedToCollection}=useStore();const [modal,setModal]=useState(false);if(!selected.length)return null;return <><div className="selection-bar"><div className="text-sm font-medium mr-2"><span className="inline-grid place-items-center bg-acid text-ink rounded-full h-7 w-7 mr-2">{selected.length}</span> selected</div><button onClick={()=>setModal(true)}><FolderPlus/> Add to collection</button><button onClick={()=>setSelectedVisibility('public')}><Eye/> Make public</button><button onClick={archiveSelected}><Archive/> Archive</button><button onClick={clearSelected} className="ml-auto"><X/> Clear</button></div><Modal open={modal} onClose={()=>setModal(false)} title="Add to collection"><div className="space-y-2">{collections.map(c=><button key={c.id} className="w-full border border-black/10 rounded-xl p-4 text-left hover:bg-black/5" onClick={()=>{addSelectedToCollection(c.id);setModal(false)}}><div className="font-medium">{c.name}</div><div className="text-sm text-black/45">{c.projectIds.length} projects</div></button>)}</div></Modal></>}
