import {ArrowLeft,BookOpen,FileText,Layers3,Presentation,Users} from 'lucide-react'
import {useCallback,useEffect,useMemo,useState} from 'react'
import {useNavigate,useSearchParams} from 'react-router-dom'
import {useAuth} from '../auth'
import {apiRequest} from '../lib/api'
import {createUuid} from '../lib/id'
import {approvedDraftSections,projectKnowledgeStatus,reviewedKnowledgeFacts} from '../lib/project'
import {supabase} from '../lib/supabase'
import {useStore} from '../store'
import {Asset,Project,StudioPackage,StudioPackageMode,StudioPackageType,StudioSection} from '../types'

type Flow='home'|'sheet'|'collection'|'cv'|'tender'
type SheetVariant='hero'|'plan'|'collage'
const labels:Record<StudioPackageType,string>={single_project_sheet:'Project Sheet',project_collection:'Project Collection',pitch:'Pitch',cv:'CV',tender:'Tender Response'}
const source=(project:Project,type='approved_project')=>({sourceType:type,sourceId:project.id,sourceProjectId:project.id,sourceStatus:'approved',confidentialityStatus:project.confidentiality})
const section=(sectionType:string,title:string,body:string,order:number,sources:StudioSection['sources']=[]):StudioSection=>({id:createUuid(),sectionType,sectionOrder:order,title,body,status:'draft',sources})
const imageAssets=(project:Project)=>project.assets.filter(asset=>asset.url&&['hero','photo','render','plan','section','diagram'].includes(asset.type))
const projectSummary=(project:Project)=>approvedDraftSections(project).find(item=>item.key==='summary')?.value||project.story.brief
const projectNarrative=(project:Project)=>approvedDraftSections(project).find(item=>item.label==='Project narrative')?.value||project.story.response
const eligibleProject=(project:Project,mode:StudioPackageMode)=>projectKnowledgeStatus(project)==='Ready for Studio'&&project.status!=='Archived'&&(mode==='internal'||project.confidentiality!=='internal-only')
const capabilityIntroduction=(projects:Project[],purpose:string)=>{const sectors=[...new Set(projects.map(project=>project.sector).filter(Boolean))],services=[...new Set(projects.flatMap(project=>project.services).filter(Boolean))].slice(0,5),conditions=[...new Set(projects.flatMap(project=>project.tags).filter(Boolean))].slice(0,4);return [`This collection brings together ${projects.length} approved project precedent${projects.length===1?'':'s'}${sectors.length?` across ${sectors.join(', ')}`:''}.`,services.length?`Together, the work demonstrates applied capability in ${services.join(', ')}.`:'',conditions.length?`The selected projects provide grounded evidence across ${conditions.join(', ')}.`:'',purpose?`The collection is curated for ${purpose.trim().replace(/[.]$/,'')}.`:'' ].filter(Boolean).join(' ')}
const sampleTender={tender_title:'Regional Places and Precinct Strategy',client:'Sample public-sector client',client_needs:'A clear place-led strategy supported by relevant precinct planning and engagement experience.',evaluation_criteria:['Demonstrated place and precinct strategy experience','Evidence of stakeholder and community engagement','A credible approach to public-domain outcomes'],mandatory_requirements:['Nominate relevant project precedents','Identify the proposed team and their explicit roles'],capability_to_prove:['Place strategy','Urban design','Stakeholder engagement'],key_decision_factors:['Direct relevance of precedents','Clarity of individual experience','Evidence-backed public benefit'],team_evidence_required:['Explicit project role','Approved contribution'],evidence_gaps:['No evidence should be claimed for qualifications or mandatory criteria not recorded in Folion.']}

export default function StudioV2(){
 const {session}=useAuth(),{projects,people}=useStore(),navigate=useNavigate(),[params,setParams]=useSearchParams()
 const [packages,setPackages]=useState<StudioPackage[]>([]),[active,setActive]=useState<StudioPackage|null>(null),[flow,setFlow]=useState<Flow>('home'),[error,setError]=useState(''),[loading,setLoading]=useState(true)
 const load=useCallback(async()=>{if(!session)return;setLoading(true);try{const result=await apiRequest<{packages:StudioPackage[]}>(session,'/v1/packages');setPackages(result.packages);const id=params.get('package');setActive(id?result.packages.find(item=>item.id===id)||null:null);setFlow('home');setError('')}catch(reason){setError(reason instanceof Error?reason.message:'Studio could not load saved packages')}finally{setLoading(false)}},[session,params])
 useEffect(()=>{void load()},[load])
 const open=(item:StudioPackage)=>{setActive(item);setParams({package:item.id})}
 const close=()=>{setActive(null);setParams({});setFlow('home')}
 const created=(item:StudioPackage)=>{setPackages(current=>[item,...current.filter(value=>value.id!==item.id)]);open(item)}
 if(active)return <PackageWorkspace item={active} projects={projects} people={people} onBack={close}/>
 if(flow==='sheet')return <ProjectSheetSetup projects={projects} onBack={()=>setFlow('home')} onCreated={created}/>
 if(flow==='collection')return <CollectionSetup projects={projects} onBack={()=>setFlow('home')} onCreated={created}/>
 if(flow==='cv')return <CvSetup projects={projects} people={people} onBack={()=>setFlow('home')} onCreated={created}/>
 if(flow==='tender')return <TenderSetupNew projects={projects} people={people} onBack={()=>setFlow('home')} onCreated={created}/>
 return <div className="sv2"><header className="sv2-intro"><p className="eyebrow">Studio V2</p><h1>What are you making?</h1><p>Compose editorial packages from approved project knowledge and selected source assets.</p></header>{error&&<p className="auth-message error">{error}</p>}
  <div className="sv2-choices">
   <Choice icon={FileText} title="Project Sheet" body="One portrait A4 project story." onClick={()=>setFlow('sheet')}/>
   <Choice icon={Layers3} title="Project Collection" body="A capability introduction followed by project sheets." onClick={()=>setFlow('collection')}/>
   <Choice icon={Presentation} title="Pitch" body="Pitch visual framework in development." disabled/>
   <Choice icon={Users} title="CV" body="Role-safe experience tailored to a brief." onClick={()=>setFlow('cv')}/>
   <Choice icon={BookOpen} title="Tender Response" body="Understand a tender and assemble grounded evidence." onClick={()=>setFlow('tender')}/>
  </div>
  <section className="sv2-saved"><p className="eyebrow">Saved work</p><h2>Packages</h2>{loading?<p>Loading packages…</p>:!packages.length?<p>No Studio V2 packages yet.</p>:<div>{packages.filter(item=>String(item.data.template||'').startsWith('studio-v2')).map(item=><button key={item.id} onClick={()=>open(item)}><span><strong>{item.title}</strong><small>{labels[item.packageType]} · {item.mode}</small></span><span>{new Date(item.updatedAt).toLocaleDateString()}</span></button>)}</div>}</section>
 </div>
}

function Choice({icon:Icon,title,body,onClick,disabled=false}:{icon:typeof FileText;title:string;body:string;onClick?:()=>void;disabled?:boolean}){return <button disabled={disabled} onClick={onClick}><Icon/><span><strong>{title}</strong><small>{body}</small></span>{disabled&&<em>On hold</em>}</button>}

function ProjectSheetSetup({projects,onBack,onCreated}:{projects:Project[];onBack:()=>void;onCreated:(item:StudioPackage)=>void}){
 const {session}=useAuth();const [mode,setMode]=useState<StudioPackageMode>('internal'),[projectId,setProjectId]=useState(''),[variant,setVariant]=useState<SheetVariant>('hero'),[primary,setPrimary]=useState(''),[support,setSupport]=useState<string[]>([]),[saving,setSaving]=useState(false),[error,setError]=useState('')
 const eligible=projects.filter(item=>eligibleProject(item,mode)),project=eligible.find(item=>item.id===projectId),assets=project?imageAssets(project):[]
 useEffect(()=>{if(project&&!primary)setPrimary(assets[0]?.id||'')},[project,assets,primary])
 const create=async()=>{if(!session||!project)return setError('Select one project that is Ready for Studio.');setSaving(true);try{const facts=reviewedKnowledgeFacts(project),sections=[section('project_summary','Project description',projectSummary(project),0,[source(project,'approved_project_summary')]),section('project_narrative','Project narrative',projectNarrative(project),1,[source(project,'approved_project_narrative')]),section('project_facts','Project facts',facts.map(item=>`${item.label}: ${item.value}`).join('\n'),2,facts.map(()=>source(project,'approved_project_fact'))),section('outcomes','Outcomes and relevance',[project.story.outcome,project.whyItMatters].filter(Boolean).join('\n\n'),3,[source(project)])];const result=await apiRequest<{package:StudioPackage}>(session,'/v1/packages',{method:'POST',body:JSON.stringify({packageType:'single_project_sheet',title:project.projectName,mode,state:'draft',data:{template:'studio-v2-project-sheet',variant,projectId:project.id,primaryAssetId:primary,supportAssetIds:support},sections,projectIds:[project.id],personIds:[],assetIds:[primary,...support].filter(Boolean)})});onCreated(result.package)}catch(reason){setError(reason instanceof Error?reason.message:'Project Sheet could not be created')}finally{setSaving(false)}}
 return <Setup title="Create a Project Sheet" onBack={onBack} error={error}><Mode value={mode} onChange={value=>{setMode(value);setProjectId('');setPrimary('');setSupport([])}}/><Field title="Choose one Ready for Studio project">{eligible.map(item=><SelectRow key={item.id} checked={projectId===item.id} type="radio" title={item.projectName} detail={`${item.location} · ${item.confidentiality.replaceAll('-',' ')}`} onChange={()=>{setProjectId(item.id);setPrimary('');setSupport([])}}/>)}</Field><Field title="Page composition"><div className="sv2-variant">{(['hero','plan','collage'] as SheetVariant[]).map(value=><button className={variant===value?'active':''} onClick={()=>setVariant(value)} key={value}>{value==='hero'?'Hero-led':value==='plan'?'Plan / map-led':'Collage-led'}</button>)}</div></Field>{project&&<Field title="Select visual material"><div className="sv2-assets">{assets.map(asset=><label key={asset.id}><input type="radio" name="primary" checked={primary===asset.id} onChange={()=>setPrimary(asset.id)}/><img src={asset.url} alt=""/><span>{asset.title||asset.fileName||asset.type}<small>Primary image</small></span></label>)}</div>{variant==='collage'&&<div className="sv2-support"><p>Supporting images (up to two)</p>{assets.filter(asset=>asset.id!==primary).map(asset=><SelectRow key={asset.id} checked={support.includes(asset.id)} type="checkbox" title={asset.title||asset.fileName||asset.type} detail={asset.type} onChange={()=>setSupport(current=>current.includes(asset.id)?current.filter(id=>id!==asset.id):current.length<2?[...current,asset.id]:current)}/>)}</div>}</Field>}<button className="sv2-primary" disabled={saving||!project} onClick={create}>{saving?'Creating…':'Create Project Sheet'}</button></Setup>
}

function CollectionSetup({projects,onBack,onCreated}:{projects:Project[];onBack:()=>void;onCreated:(item:StudioPackage)=>void}){return <PlaceholderSetup title="Create a Project Collection" projects={projects} packageType="project_collection" template="studio-v2-collection" onBack={onBack} onCreated={onCreated}/>} 
function PitchSetup({projects,onBack,onCreated}:{projects:Project[];onBack:()=>void;onCreated:(item:StudioPackage)=>void}){
 const {session}=useAuth();const [mode,setMode]=useState<StudioPackageMode>('internal'),[briefing,setBriefing]=useState(''),[projectIds,setProjectIds]=useState<string[]>([]),[saving,setSaving]=useState(false),[error,setError]=useState('');const eligible=projects.filter(project=>eligibleProject(project,mode));const toggle=(id:string)=>setProjectIds(current=>current.includes(id)?current.filter(value=>value!==id):[...current,id]);const create=async()=>{if(!session)return;if(!briefing.trim())return setError('Enter a Pitch Briefing.');if(!projectIds.length)return setError('Select at least one approved project as evidence.');setSaving(true);try{const selected=projectIds.map(id=>projects.find(project=>project.id===id)).filter(Boolean) as Project[],lead=selected[0],services=[...new Set(selected.flatMap(project=>project.services))].slice(0,5),slides=[{kind:'cover',title:briefing.trim().split(/[.!?]/)[0].slice(0,90)||'A proposition for place',body:'A focused proposition grounded in approved project intelligence.',projectId:lead.id,assetId:imageAssets(lead)[0]?.id},{kind:'tension',title:'The defining opportunity',body:briefing.trim(),projectId:lead.id,assetId:imageAssets(lead)[0]?.id},{kind:'success',title:'What success needs to achieve',body:[lead.story.challenge,lead.whyItMatters].filter(Boolean).join(' '),projectId:lead.id,assetId:imageAssets(lead)[1]?.id||imageAssets(lead)[0]?.id},{kind:'precedent',title:'Precedent that makes the ambition tangible',body:projectNarrative(lead),projectId:lead.id,assetId:imageAssets(lead)[0]?.id},...selected.slice(1,3).map((project,index)=>({kind:'evidence',title:index?'Evidence for enduring relevance':'A response grounded in place',body:[project.story.response,project.story.outcome].filter(Boolean).join(' '),projectId:project.id,assetId:imageAssets(project)[0]?.id})),{kind:'proof',title:'Capability assembled around the brief',body:services.length?services.join(' · '):selected.map(project=>project.sector).filter(Boolean).join(' · '),projectId:lead.id,assetId:imageAssets(lead)[2]?.id||imageAssets(lead)[0]?.id},{kind:'close',title:'A clear next move',body:'Apply the strongest approved precedent to the opportunity, with the evidence and ambition held together as one story.',projectId:lead.id,assetId:imageAssets(lead)[0]?.id}].slice(0,8);const sections=slides.map((slide,index)=>section('pitch_slide',slide.title,slide.body,index,[source(projects.find(project=>project.id===slide.projectId)!)]));const result=await apiRequest<{package:StudioPackage}>(session,'/v1/packages',{method:'POST',body:JSON.stringify({packageType:'pitch',title:slides[0].title,mode,state:'draft',data:{template:'studio-v2-pitch',briefing,slides},sections,projectIds,personIds:[],assetIds:slides.map(slide=>slide.assetId).filter(Boolean)})});onCreated(result.package)}catch(reason){setError(reason instanceof Error?reason.message:'Pitch package could not be created')}finally{setSaving(false)}};return <Setup title="Create a Pitch Package" onBack={onBack} error={error}><Mode value={mode} onChange={value=>{setMode(value);setProjectIds([])}}/><label className="label">Pitch Briefing<textarea value={briefing} onChange={event=>setBriefing(event.target.value)} placeholder="Describe what is being pitched, the opportunity, tension and desired narrative."/></label><Field title="Approved projects used as narrative evidence">{eligible.map(project=><SelectRow key={project.id} checked={projectIds.includes(project.id)} type="checkbox" title={project.projectName} detail={`${project.sector} · ${project.location}`} onChange={()=>toggle(project.id)}/>)}</Field><button className="sv2-primary" disabled={saving} onClick={create}>{saving?'Composing…':'Create visual narrative'}</button></Setup>
}
function CvSetup({projects,people,onBack,onCreated}:{projects:Project[];people:{id:string;name:string;position:string;bio?:string;skills?:string[]}[];onBack:()=>void;onCreated:(item:StudioPackage)=>void}){
 const {session}=useAuth();const [mode,setMode]=useState<StudioPackageMode>('internal'),[personIds,setPersonIds]=useState<string[]>([]),[projectIds,setProjectIds]=useState<string[]>([]),[brief,setBrief]=useState(''),[saving,setSaving]=useState(false),[error,setError]=useState('')
 const score=(project:Project)=>{const words=brief.toLowerCase().split(/\W+/).filter(word=>word.length>3);return words.filter(word=>[project.projectName,project.sector,project.location,...project.services,...project.tags].join(' ').toLowerCase().includes(word)).length}
 const roleSafe=useMemo(()=>projects.filter(project=>eligibleProject(project,mode)&&project.team.some(member=>personIds.includes(member.personId)&&member.projectRole.trim())).sort((a,b)=>score(b)-score(a)),[projects,personIds,mode,brief])
 const togglePerson=(id:string)=>{const next=personIds.includes(id)?personIds.filter(value=>value!==id):[...personIds,id];setPersonIds(next);const recommended=projects.filter(project=>eligibleProject(project,mode)&&project.team.some(member=>next.includes(member.personId)&&member.projectRole.trim())).sort((a,b)=>score(b)-score(a)).slice(0,5).map(project=>project.id);setProjectIds(recommended)}
 const create=async()=>{if(!session||!personIds.length)return setError('Select a person or proposed team.');if(!projectIds.length)return setError('No explicit project role has been selected.');setSaving(true);try{const chosen=projectIds.map(id=>projects.find(project=>project.id===id)).filter(Boolean) as Project[];const profiles=people.filter(person=>personIds.includes(person.id));const summary=brief.trim()?`For this opportunity, the selected team brings explicitly recorded experience across ${[...new Set(chosen.map(project=>project.sector).filter(Boolean))].join(', ')}. The project evidence below has been selected against the brief and retains each person's recorded project role.`:`The selected experience below is drawn only from explicit project-team assignments and approved project knowledge.`;const sections=[section('cv_profile','Bespoke Experience Summary',summary,0,personIds.map(id=>({sourceType:'approved_person_profile',sourceId:id,sourcePersonId:id,sourceStatus:'approved'}))),...chosen.flatMap((project,index)=>project.team.filter(member=>personIds.includes(member.personId)&&member.projectRole.trim()).map(member=>section('cv_experience',`${member.name} · ${project.projectName}`,[`Project-specific role: ${member.projectRole}`,member.contribution&&`Approved contribution: ${member.contribution}`,projectSummary(project),brief&&`Brief relevance: ${project.sector}${project.services.length?` · ${project.services.join(', ')}`:''}`].filter(Boolean).join('\n\n'),index+1,[{...source(project,'explicit_project_role'),sourcePersonId:member.personId}])) )];const result=await apiRequest<{package:StudioPackage}>(session,'/v1/packages',{method:'POST',body:JSON.stringify({packageType:'cv',title:profiles.length===1?`${profiles[0].name} · Bespoke CV`:'Proposed team CVs',mode,state:'draft',data:{template:'studio-v2-cv',brief,recommendedCount:5},sections,projectIds,personIds,assetIds:[]})});onCreated(result.package)}catch(reason){setError(reason instanceof Error?reason.message:'CV package could not be created')}finally{setSaving(false)}}
 return <Setup title="Create a CV" onBack={onBack} error={error}><Mode value={mode} onChange={value=>{setMode(value);setProjectIds([])}}/><label className="label">Opportunity brief or context <small>(optional for a general CV)</small><textarea value={brief} onChange={event=>setBrief(event.target.value)}/></label><Field title="Person or proposed team">{people.map(person=><SelectRow key={person.id} checked={personIds.includes(person.id)} type="checkbox" title={person.name} detail={person.position} onChange={()=>togglePerson(person.id)}/>)}</Field>{personIds.length>0&&<Field title="Selected experience · five recommended by default">{roleSafe.map(project=><SelectRow key={project.id} checked={projectIds.includes(project.id)} type="checkbox" title={project.projectName} detail={`${project.team.filter(member=>personIds.includes(member.personId)).map(member=>member.projectRole).join(', ')} · ${project.location}`} onChange={()=>setProjectIds(current=>current.includes(project.id)?current.filter(id=>id!==project.id):[...current,project.id])}/>)}</Field>}<button className="sv2-primary" disabled={saving} onClick={create}>{saving?'Creating…':'Create bespoke CV'}</button></Setup>
} 
function TenderSetup({projects,people,onBack,onCreated}:{projects:Project[];people:{id:string;name:string;position:string}[];onBack:()=>void;onCreated:(item:StudioPackage)=>void}){
 const {session}=useAuth(),{workspace}=useStore();const [mode,setMode]=useState<StudioPackageMode>('internal'),[file,setFile]=useState<File|null>(null),[count,setCount]=useState(3),[projectIds,setProjectIds]=useState<string[]>([]),[personIds,setPersonIds]=useState<string[]>([]),[saving,setSaving]=useState(false),[error,setError]=useState('');const eligible=projects.filter(project=>eligibleProject(project,mode));const toggle=(id:string,setter:React.Dispatch<React.SetStateAction<string[]>>)=>setter(current=>current.includes(id)?current.filter(value=>value!==id):[...current,id]);const create=async()=>{if(!session||!workspace||!supabase)return setError('A workspace session is required.');if(!file)return setError('Upload a text-based tender PDF.');if(file.type!=='application/pdf')return setError('Tender source must be a PDF.');setSaving(true);try{const selectedProjects=projectIds.slice(0,count),sections=[section('tender_understanding','Tender understanding','Analysis is being prepared from the uploaded tender.',0,[]),section('firm_statement','Why this firm is right for this tender','Draft after the Tender Understanding is reviewed.',1,selectedProjects.map(id=>source(projects.find(project=>project.id===id)!))),...selectedProjects.map((id,index)=>{const project=projects.find(value=>value.id===id)!;return section('project_precedent',project.projectName,projectSummary(project),index+2,[source(project)])})];const result=await apiRequest<{package:StudioPackage}>(session,'/v1/packages',{method:'POST',body:JSON.stringify({packageType:'tender',title:file.name.replace(/\.pdf$/i,''),mode,state:'draft',data:{template:'studio-v2-tender',projectCount:count,tenderSourceName:file.name},sections,projectIds:selectedProjects,personIds,assetIds:[]})});const signed=await apiRequest<{sourceId:string;storagePath:string;token:string}>(session,`/v1/packages/${encodeURIComponent(result.package.id)}/tender-source/upload-url`,{method:'POST',body:JSON.stringify({filename:file.name})});const {error:uploadError}=await supabase.storage.from('project-assets').uploadToSignedUrl(signed.storagePath,signed.token,file,{contentType:file.type});if(uploadError)throw uploadError;await apiRequest(session,`/v1/packages/${encodeURIComponent(result.package.id)}/tender-source`,{method:'POST',body:JSON.stringify({sourceId:signed.sourceId,storagePath:signed.storagePath,filename:file.name,mimeType:file.type,fileSize:file.size})});onCreated(result.package)}catch(reason){setError(reason instanceof Error?reason.message:'Tender package could not be created')}finally{setSaving(false)}};return <Setup title="Create a Tender Response" onBack={onBack} error={error}><Mode value={mode} onChange={value=>{setMode(value);setProjectIds([])}}/><label className="label">Tender PDF<input type="file" accept="application/pdf" onChange={event=>setFile(event.target.files?.[0]||null)}/><small>Folion extracts every readable page server-side. Scanned PDFs are not supported in this version.</small></label><label className="label">Number of project precedents<input type="number" min="1" max="12" value={count} onChange={event=>setCount(Number(event.target.value))}/></label><Field title="Eligible approved projects">{eligible.map(project=><SelectRow key={project.id} checked={projectIds.includes(project.id)} type="checkbox" title={project.projectName} detail={`${project.sector} · ${project.location}`} onChange={()=>toggle(project.id,setProjectIds)}/>)}</Field><Field title="Proposed team">{people.map(person=><SelectRow key={person.id} checked={personIds.includes(person.id)} type="checkbox" title={person.name} detail={person.position} onChange={()=>toggle(person.id,setPersonIds)}/>)}</Field><button className="sv2-primary" disabled={saving} onClick={create}>{saving?'Uploading tender…':'Create and analyse tender'}</button></Setup>
} 

function PlaceholderSetup({title,projects,people=[],packageType,template,onBack,onCreated}:{title:string;projects:Project[];people?:{id:string;name:string;position:string}[];packageType:StudioPackageType;template:string;onBack:()=>void;onCreated:(item:StudioPackage)=>void}){
 const {session}=useAuth();const [mode,setMode]=useState<StudioPackageMode>('internal'),[selected,setSelected]=useState<string[]>([]),[selectedPeople,setSelectedPeople]=useState<string[]>([]),[brief,setBrief]=useState(''),[count,setCount]=useState(5),[saving,setSaving]=useState(false),[error,setError]=useState('');const eligible=projects.filter(item=>eligibleProject(item,mode));const toggle=(id:string,setter:React.Dispatch<React.SetStateAction<string[]>>)=>setter(current=>current.includes(id)?current.filter(value=>value!==id):[...current,id]);const move=(id:string,direction:-1|1)=>setSelected(current=>{const index=current.indexOf(id),next=index+direction;if(index<0||next<0||next>=current.length)return current;const copy=[...current];[copy[index],copy[next]]=[copy[next],copy[index]];return copy});const create=async()=>{if(!session||!selected.length)return setError('Select at least one approved project.');if(packageType==='cv'&&!selectedPeople.length)return setError('Select at least one person.');setSaving(true);try{const chosen=selected.map(id=>projects.find(item=>item.id===id)).filter(Boolean) as Project[];const sections=chosen.map((project,index)=>section('project_sheet',project.projectName,[projectSummary(project),projectNarrative(project)].filter(Boolean).join('\n\n'),index+1,[source(project)]));if(packageType==='project_collection')sections.unshift(section('capability_intro','Capability introduction',capabilityIntroduction(chosen,brief),0,chosen.map(item=>source(item))));if(packageType==='cv')sections.unshift(section('cv_profile','Bespoke profile',brief,0,selectedPeople.map(id=>({sourceType:'approved_person_profile',sourceId:id,sourcePersonId:id,sourceStatus:'approved'}))));if(packageType==='tender')sections.unshift(section('tender_understanding','Tender understanding',brief,0,[]));const result=await apiRequest<{package:StudioPackage}>(session,'/v1/packages',{method:'POST',body:JSON.stringify({packageType,title,mode,state:'draft',data:{template,brief,projectCount:count},sections,projectIds:selected,personIds:selectedPeople,assetIds:chosen.map(project=>imageAssets(project)[0]?.id).filter(Boolean)})});onCreated(result.package)}catch(reason){setError(reason instanceof Error?reason.message:'Package could not be created')}finally{setSaving(false)}};return <Setup title={title} onBack={onBack} error={error}><Mode value={mode} onChange={setMode}/><label className="label">Brief / purpose<textarea value={brief} onChange={event=>setBrief(event.target.value)}/></label>{packageType==='tender'&&<label className="label">Tender PDF<input type="file" accept="application/pdf" onChange={event=>setBrief(current=>current||`Tender source: ${event.target.files?.[0]?.name||''}`)}/><small>Stored as a package source when the tender is created.</small></label>}{packageType==='tender'&&<label className="label">Recommended project count<input type="number" min="1" max="12" value={count} onChange={event=>setCount(Number(event.target.value))}/></label>}{people.length>0&&<Field title="People">{people.map(person=><SelectRow key={person.id} checked={selectedPeople.includes(person.id)} type="checkbox" title={person.name} detail={person.position} onChange={()=>toggle(person.id,setSelectedPeople)}/>)}</Field>}<Field title={packageType==='cv'?'Role-safe project experience':'Projects'}>{eligible.map(project=><SelectRow key={project.id} checked={selected.includes(project.id)} type="checkbox" title={project.projectName} detail={`${project.location} · ${project.confidentiality.replaceAll('-',' ')}`} onChange={()=>toggle(project.id,setSelected)}/>)}</Field>{packageType==='project_collection'&&selected.length>1&&<section className="sv2-order"><h3>Page order</h3>{selected.map((id,index)=><div key={id}><span>{index+1}. {projects.find(project=>project.id===id)?.projectName}</span><button disabled={index===0} onClick={()=>move(id,-1)}>↑</button><button disabled={index===selected.length-1} onClick={()=>move(id,1)}>↓</button></div>)}</section>}<button className="sv2-primary" disabled={saving} onClick={create}>{saving?'Creating…':'Create package'}</button></Setup>}

function PackageWorkspace({item,projects,people,onBack}:{item:StudioPackage;projects:Project[];people:{id:string;name:string;position:string;bio?:string;skills?:string[]}[];onBack:()=>void}){const project=projects.find(value=>value.id===item.projectIds[0]);return <div className="sv2-workspace"><header><button onClick={onBack}><ArrowLeft/>Studio V2</button><div><p className="eyebrow">{labels[item.packageType]}</p><h1>{item.title}</h1></div><span>Saved · {new Date(item.updatedAt).toLocaleString()}</span></header><main>{item.packageType==='single_project_sheet'&&project?<ProjectSheetPageNew item={item} project={project}/>:item.packageType==='project_collection'?<CollectionPages item={item} projects={projects}/>:item.packageType==='cv'?<CvPagesNew item={item} projects={projects} people={people}/>:<TenderPagesNew item={item} projects={projects} people={people}/>}</main></div>}

function ProjectSheetPage({item,project}:{item:StudioPackage;project:Project}){const primary=project.assets.find(asset=>asset.id===item.data.primaryAssetId)||imageAssets(project)[0],support=(item.data.supportAssetIds as string[]||[]).map(id=>project.assets.find(asset=>asset.id===id)).filter(Boolean) as Asset[],variant=String(item.data.variant||'hero');const facts=reviewedKnowledgeFacts(project).slice(0,6),summary=item.sections.find(value=>value.sectionType==='project_summary')?.body||projectSummary(project),narrative=item.sections.find(value=>value.sectionType==='project_narrative')?.body||projectNarrative(project),outcomes=item.sections.find(value=>value.sectionType==='outcomes')?.body||[project.story.outcome,project.whyItMatters].filter(Boolean).join('\n\n');return <article className={`sv2-page sv2-sheet ${variant}`}><Visual primary={primary} support={support}/><section className="sv2-title"><p>{project.sector}</p><h2>{project.projectName}<i>+</i></h2></section><section className="sv2-body"><aside>{facts.map(fact=><div key={fact.key}><small>{fact.label}</small><strong>{fact.value}</strong></div>)}</aside><div><h3>Project Description</h3><p>{summary}</p><p>{narrative}</p></div><div><h3>Outcomes + Relevance</h3><p>{outcomes||'No approved outcome statement has been selected.'}</p></div></section><footer>Folion · Approved project knowledge</footer></article>}
function Visual({primary,support}:{primary?:Asset;support:Asset[]}){return <div className="sv2-visual">{primary?.url?<img src={primary.url} alt={primary.caption||primary.title}/>:<div className="sv2-image-placeholder">Approved visual not selected</div>}{support.map(asset=><img key={asset.id} src={asset.url} alt={asset.caption||asset.title}/>)}</div>}
function CollectionPages({item,projects}:{item:StudioPackage;projects:Project[]}){const ordered=item.projectIds.map(id=>projects.find(project=>project.id===id)).filter(Boolean) as Project[];return <>{<article className="sv2-page sv2-intro-page"><p className="eyebrow">Project Collection</p><h2>{item.title}</h2><p>{item.sections.find(value=>value.sectionType==='capability_intro')?.body}</p><strong>{ordered.length} selected projects</strong></article>}{ordered.map(project=><ProjectSheetPageNew key={project.id} item={{...item,data:{...item.data,primaryAssetId:imageAssets(project)[0]?.id}}} project={project}/>)}</>}
function CvPages({item,projects,people}:{item:StudioPackage;projects:Project[];people:{id:string;name:string;position:string;bio?:string;skills?:string[]}[]}){return <>{people.filter(value=>item.personIds.includes(value.id)).map(person=><article className="sv2-page sv2-cv" key={person.id}><header><div><h2>{person.name}</h2><p>{person.position}</p></div><strong>FOLION</strong></header><section><aside><h3>Expertise</h3><p>{person.skills?.join('\n')||'Approved expertise not recorded.'}</p></aside><main><h3>Experience Summary</h3><p>{item.sections.find(value=>value.sectionType==='cv_profile')?.body||person.bio}</p><h3>Selected Experience</h3>{projects.filter(project=>item.projectIds.includes(project.id)&&project.team.some(member=>member.personId===person.id&&member.projectRole.trim())).slice(0,5).map(project=>{const member=project.team.find(value=>value.personId===person.id)!;return <div key={project.id}><h4>{project.client?`${project.client}, `:''}{project.projectName}, {project.location} — {member.projectRole}</h4>{member.contribution&&<p>{member.contribution}</p>}<p>{projectSummary(project)}</p></div>})}</main></section></article>)}</>}
function TenderPages({item,projects,people}:{item:StudioPackage;projects:Project[];people:{id:string;name:string;position:string}[]}){const {session}=useAuth();const [job,setJob]=useState<{status:string;analysis?:Record<string,unknown>;failure_reason?:string}|null>(null);useEffect(()=>{if(!session)return;let stopped=false,timer:ReturnType<typeof setTimeout>;const poll=async()=>{try{const result=await apiRequest<{job:typeof job}>(session,`/v1/packages/${encodeURIComponent(item.id)}/tender-analysis`);if(!stopped)setJob(result.job);if(result.job&&['queued','extracting_text','analysing'].includes(result.job.status))timer=setTimeout(poll,2500)}catch{} };void poll();return()=>{stopped=true;clearTimeout(timer)}},[session,item.id]);const selected=projects.filter(project=>item.projectIds.includes(project.id)),analysis=job?.analysis||{},list=(key:string)=>Array.isArray(analysis[key])?(analysis[key] as string[]):[],firmStatement=`The selected evidence brings together approved experience across ${[...new Set(selected.map(project=>project.sector).filter(Boolean))].join(', ')||'the required project contexts'}${selected.flatMap(project=>project.services).length?`, supported by recorded services in ${[...new Set(selected.flatMap(project=>project.services))].slice(0,5).join(', ')}`:''}. Each precedent and team role remains traceable to approved Folion records.`;return <div className="sv2-tender-layout"><article className="sv2-page sv2-tender"><p className="eyebrow">Tender response</p><h2>{String(analysis.tender_title||item.title)}</h2><h3>Why this firm is right for this tender</h3><p>{firmStatement}</p><h3>Relevant project precedents</h3>{selected.map(project=><div key={project.id}><strong>{project.projectName}</strong><p>{projectSummary(project)}</p></div>)}{item.personIds.length>0&&<><h3>Proposed team</h3><p>{people.filter(person=>item.personIds.includes(person.id)).map(person=>person.name).join(', ')}</p></>}</article><aside className="sv2-inspector"><p className="eyebrow">Internal only</p><h3>Tender Understanding</h3>{!job?<p>Waiting for analysis…</p>:job.status==='failed'?<p className="auth-message error">{job.failure_reason}</p>:job.status!=='ready_for_review'?<p>Folion is {job.status.replaceAll('_',' ')}…</p>:<><h4>What the client needs</h4><p>{String(analysis.client_needs||'Not stated')}</p><h4>Capability to prove</h4><ul>{list('capability_to_prove').map(value=><li key={value}>{value}</li>)}</ul><h4>Decision factors</h4><ul>{list('key_decision_factors').map(value=><li key={value}>{value}</li>)}</ul><h4>Team evidence</h4><ul>{list('team_evidence_required').map(value=><li key={value}>{value}</li>)}</ul><h4>Evidence gaps</h4><ul>{list('evidence_gaps').map(value=><li key={value}>{value}</li>)}</ul></>}</aside></div>}

function PitchPages({item,projects}:{item:StudioPackage;projects:Project[]}){const slides=Array.isArray(item.data.slides)?item.data.slides as Array<{kind:string;title:string;body:string;projectId:string;assetId?:string}>:[],[active,setActive]=useState<number|null>(null);const render=(slide:typeof slides[number],index:number,preview=false)=>{const project=projects.find(value=>value.id===slide.projectId),asset=project?.assets.find(value=>value.id===slide.assetId);return <article className={`sv2-slide ${slide.kind} ${preview?'preview':''}`} key={`${slide.title}-${index}`}>{asset?.url&&<img src={asset.url} alt=""/>}<div><span>{String(index+1).padStart(2,'0')}</span><h2>{slide.title}</h2><p>{slide.body}</p></div></article>};return <div className="sv2-deck"><header><div><p className="eyebrow">Deck overview</p><h2>{slides.length} slide visual narrative</h2></div><button className="sv2-primary" onClick={()=>setActive(0)} disabled={!slides.length}>Present deck</button></header><div className="sv2-slide-grid">{slides.map((slide,index)=><button key={`${slide.title}-${index}`} onClick={()=>setActive(index)}>{render(slide,index)}</button>)}</div>{active!==null&&slides[active]&&<div className="sv2-present"><button className="sv2-present-close" onClick={()=>setActive(null)}>Close</button>{render(slides[active],active,true)}<nav><button disabled={active===0} onClick={()=>setActive(value=>Math.max(0,(value||0)-1))}>Previous</button><span>{active+1} / {slides.length}</span><button disabled={active===slides.length-1} onClick={()=>setActive(value=>Math.min(slides.length-1,(value||0)+1))}>Next</button></nav></div>}</div>}

function ProjectSheetPageNew({item,project}:{item:StudioPackage;project:Project}){const primary=project.assets.find(asset=>asset.id===item.data.primaryAssetId)||imageAssets(project)[0],support=(item.data.supportAssetIds as string[]||[]).map(id=>project.assets.find(asset=>asset.id===id)).filter(Boolean) as Asset[],variant=String(item.data.variant||'hero'),facts=[{label:'Client',value:project.client},{label:'Location',value:project.location},{label:'Year',value:project.year},{label:'Services',value:project.services.join(', ')},{label:'Role',value:project.identity?.role?.join(', ')},{label:'Site area',value:project.siteArea},{label:'GFA',value:project.gfa},{label:'Height',value:project.height}].filter(fact=>fact.value?.trim()),summary=item.sections.find(value=>value.sectionType==='project_summary')?.body||projectSummary(project),narrative=item.sections.find(value=>value.sectionType==='project_narrative')?.body||projectNarrative(project),outcomes=item.sections.find(value=>value.sectionType==='outcomes')?.body||[project.story.outcome,project.whyItMatters].filter(Boolean).join('\n\n');return <article className={`sv2-page sv2-sheet ${variant}`}><Visual primary={primary} support={support}/><section className="sv2-title">{project.sector&&<p>{project.sector}</p>}<h2>{project.projectName}<i>+</i></h2></section><section className="sv2-body"><aside>{facts.map(fact=><div key={fact.label}><small>{fact.label}</small><strong>{fact.value}</strong></div>)}</aside><div><h3>Project Description</h3>{summary&&<p>{summary}</p>}{narrative&&<p>{narrative}</p>}</div><div><h3>Outcomes + Relevance</h3>{outcomes&&<p>{outcomes}</p>}</div></section></article>}

function CvPagesNew({item,projects,people}:{item:StudioPackage;projects:Project[];people:{id:string;name:string;position:string;bio?:string;skills?:string[]}[]}){return <>{people.filter(value=>item.personIds.includes(value.id)).flatMap(person=>{const profile=person as typeof person&{education?:string[];affiliations?:string[];yearsExperience?:string;careerHistory?:string[]},experience=projects.filter(project=>item.projectIds.includes(project.id)&&project.team.some(member=>member.personId===person.id&&member.projectRole.trim())).slice(0,5);return [<article className="sv2-page sv2-cv" key={`${person.id}-profile`}><header><div><h2>{person.name}</h2><p>{person.position}</p></div><strong>FOLION</strong></header><section><aside>{profile.education?.length?<><h3>Education</h3><p>{profile.education.join('\n')}</p></>:null}{profile.affiliations?.length?<><h3>Affiliations</h3><p>{profile.affiliations.join('\n')}</p></>:null}{profile.yearsExperience&&<><h3>Years in practice</h3><p>{profile.yearsExperience}</p></>} {person.skills?.length?<><h3>Areas of Expertise</h3><p>{person.skills.join('\n')}</p></>:null}</aside><main><h3>Experience Summary</h3><p>{item.sections.find(value=>value.sectionType==='cv_profile')?.body||person.bio}</p>{profile.careerHistory?.length?<><h3>Career History</h3><p>{profile.careerHistory.join('\n')}</p></>:null}</main></section></article>,<article className="sv2-page sv2-cv sv2-cv-experience" key={`${person.id}-experience`}><header><div><p className="eyebrow">Selected Experience</p><h2>{person.name}</h2></div><strong>02</strong></header><main>{experience.map(project=>{const member=project.team.find(value=>value.personId===person.id)!;return <section key={project.id}><h4>{project.client?`${project.client}, `:''}{project.projectName}, {project.location} — {member.projectRole}</h4><p>{member.contribution||`Recorded role: ${member.projectRole}.`}</p><p>{projectSummary(project)}</p></section>})}</main></article>]})}</>}

function TenderSetupNew({projects,people,onBack,onCreated}:{projects:Project[];people:{id:string;name:string;position:string}[];onBack:()=>void;onCreated:(item:StudioPackage)=>void}){const {session}=useAuth(),{workspace}=useStore();const [mode,setMode]=useState<StudioPackageMode>('internal'),[file,setFile]=useState<File|null>(null),[fixture,setFixture]=useState(false),[count,setCount]=useState(3),[projectIds,setProjectIds]=useState<string[]>([]),[personIds,setPersonIds]=useState<string[]>([]),[saving,setSaving]=useState(false),[error,setError]=useState(''),eligible=projects.filter(project=>eligibleProject(project,mode)),toggle=(id:string,setter:React.Dispatch<React.SetStateAction<string[]>>)=>setter(current=>current.includes(id)?current.filter(value=>value!==id):[...current,id]);const create=async()=>{if(!session||!workspace||!supabase)return setError('A workspace session is required.');if(!fixture&&!file)return setError('Upload a text-based tender PDF or use the controlled sample tender.');setSaving(true);try{const selected=projectIds.slice(0,count),title=fixture?sampleTender.tender_title:file!.name.replace(/\.pdf$/i,''),sections=[section('tender_understanding','Tender understanding',fixture?sampleTender.client_needs:'Analysis is being prepared from the uploaded tender.',0,[]),...selected.map((id,index)=>{const project=projects.find(value=>value.id===id)!;return section('project_precedent',project.projectName,projectSummary(project),index+1,[source(project)])})],result=await apiRequest<{package:StudioPackage}>(session,'/v1/packages',{method:'POST',body:JSON.stringify({packageType:'tender',title,mode,state:'draft',data:{template:'studio-v2-tender',projectCount:count,tenderSourceName:fixture?'Controlled sample tender':file!.name,tenderFixture:fixture?sampleTender:null},sections,projectIds:selected,personIds,assetIds:[]})});if(file&&!fixture){const signed=await apiRequest<{sourceId:string;storagePath:string;token:string}>(session,`/v1/packages/${encodeURIComponent(result.package.id)}/tender-source/upload-url`,{method:'POST',body:JSON.stringify({filename:file.name})}),uploaded=await supabase.storage.from('project-assets').uploadToSignedUrl(signed.storagePath,signed.token,file,{contentType:file.type});if(uploaded.error)throw uploaded.error;await apiRequest(session,`/v1/packages/${encodeURIComponent(result.package.id)}/tender-source`,{method:'POST',body:JSON.stringify({sourceId:signed.sourceId,storagePath:signed.storagePath,filename:file.name,mimeType:file.type,fileSize:file.size})})}onCreated(result.package)}catch(reason){setError(reason instanceof Error?reason.message:'Tender package could not be created')}finally{setSaving(false)}};return <Setup title="Create a Tender Response" onBack={onBack} error={error}><Mode value={mode} onChange={value=>{setMode(value);setProjectIds([])}}/><div className="sv2-source-choice"><label><input type="radio" checked={!fixture} onChange={()=>setFixture(false)}/>Upload tender PDF</label><label><input type="radio" checked={fixture} onChange={()=>setFixture(true)}/>Use controlled sample tender</label></div>{!fixture&&<label className="label">Tender PDF<input type="file" accept="application/pdf" onChange={event=>setFile(event.target.files?.[0]||null)}/><small>Every readable page is analysed server-side. Scanned PDFs are not supported.</small></label>}{fixture&&<div className="sv2-fixture"><strong>{sampleTender.tender_title}</strong><p>{sampleTender.client_needs}</p></div>}<label className="label">Number of project precedents<input type="number" min="1" max="12" value={count} onChange={event=>setCount(Number(event.target.value))}/></label><Field title="Approved projects">{eligible.map(project=><SelectRow key={project.id} checked={projectIds.includes(project.id)} type="checkbox" title={project.projectName} detail={`${project.sector} · ${project.location}`} onChange={()=>toggle(project.id,setProjectIds)}/>)}</Field><Field title="People with relevant recorded experience">{people.filter(person=>projects.some(project=>project.team.some(member=>member.personId===person.id&&member.projectRole.trim()))).map(person=><SelectRow key={person.id} checked={personIds.includes(person.id)} type="checkbox" title={person.name} detail={person.position} onChange={()=>toggle(person.id,setPersonIds)}/>)}</Field><button className="sv2-primary" disabled={saving||!projectIds.length} onClick={create}>{saving?'Preparing…':'Create tender workspace'}</button></Setup>}

function TenderPagesNew({item,projects,people}:{item:StudioPackage;projects:Project[];people:{id:string;name:string;position:string}[]}){const {session}=useAuth(),fixture=item.data.tenderFixture as typeof sampleTender|null,[job,setJob]=useState<{status:string;analysis?:Record<string,unknown>;failure_reason?:string}|null>(null);useEffect(()=>{if(!session||fixture)return;let stopped=false,timer:ReturnType<typeof setTimeout>;const poll=async()=>{try{const result=await apiRequest<{job:typeof job}>(session,`/v1/packages/${encodeURIComponent(item.id)}/tender-analysis`);if(!stopped)setJob(result.job);if(result.job&&['queued','extracting_text','analysing'].includes(result.job.status))timer=setTimeout(poll,2500)}catch{}};void poll();return()=>{stopped=true;clearTimeout(timer)}},[session,item.id,fixture]);const analysis=(fixture||job?.analysis||{}) as Record<string,unknown>,criteria=Array.isArray(analysis.evaluation_criteria)?analysis.evaluation_criteria as string[]:[],selected=item.projectIds.map(id=>projects.find(project=>project.id===id)).filter(Boolean) as Project[],selectedPeople=people.filter(person=>item.personIds.includes(person.id));const match=(criterion:string)=>{const words=criterion.toLowerCase().split(/\W+/).filter(word=>word.length>4),ranked=selected.map(project=>({project,score:words.filter(word=>[project.sector,...project.services,...project.tags,projectSummary(project)].join(' ').toLowerCase().includes(word)).length})).sort((a,b)=>b.score-a.score),best=ranked[0];return best?.score?best.project:null};return <div className="sv2-tender-review"><header><p className="eyebrow">Tender source and analysis</p><h2>{String(analysis.tender_title||item.title)}</h2><p>{String(analysis.client_needs||'Analysis is being prepared from the uploaded tender.')}</p>{!fixture&&job?.status!=='ready_for_review'&&<span className="sv2-analysis-state">{job?.status?.replaceAll('_',' ')||'waiting for analysis'}</span>}</header><section className="sv2-requirement-map"><div className="sv2-map-head"><span>Requirement</span><span>Matched evidence</span><span>Relevance and draft response</span></div>{criteria.map(criterion=>{const project=match(criterion),members=project?.team.filter(member=>item.personIds.includes(member.personId)&&member.projectRole.trim())||[];return <article key={criterion}><div><strong>{criterion}</strong><small>Source: {fixture?'Controlled sample tender':'Uploaded tender analysis'}</small></div>{project?<div><strong>{project.projectName}</strong><p>{project.services.join(', ')||project.sector}</p><small>Approved project record{members.length?` · ${members.map(member=>`${member.name}: ${member.projectRole}`).join('; ')}`:''}</small></div>:<div className="sv2-gap"><strong>Evidence gap</strong><p>No selected approved project directly supports this requirement.</p></div>}<div>{project?<><strong>Why relevant</strong><p>{projectSummary(project)}</p><strong>Draft response</strong><p>Our approved evidence includes {project.projectName}, where the recorded scope covers {project.services.join(', ')||project.sector}. {members.length?`The recorded individual experience includes ${members.map(member=>`${member.name} as ${member.projectRole}`).join(' and ')}.`:''}</p></>:<><strong>Gap — action required</strong><p>Do not claim compliance. Confirm suitable evidence or address this requirement outside Folion.</p></>}</div></article>})}{!criteria.length&&<p className="sv2-fixture">Requirements will appear here when analysis is ready.</p>}</section>{selectedPeople.length>0&&<footer>Selected people: {selectedPeople.map(person=>person.name).join(', ')}</footer>}</div>}

function Setup({title,onBack,error,children}:{title:string;onBack:()=>void;error:string;children:React.ReactNode}){return <div className="sv2 sv2-setup"><button className="detail-back" onClick={onBack}><ArrowLeft/>Studio V2</button><header><p className="eyebrow">New package</p><h1>{title}</h1><p>Choose approved knowledge and visual material. Your package remains editable and saved.</p></header>{error&&<p className="auth-message error">{error}</p>}<main>{children}</main></div>}
function Mode({value,onChange}:{value:StudioPackageMode;onChange:(value:StudioPackageMode)=>void}){return <fieldset className="sv2-mode"><legend>Use</legend><label><input type="radio" checked={value==='internal'} onChange={()=>onChange('internal')}/>Internal</label><label><input type="radio" checked={value==='external'} onChange={()=>onChange('external')}/>External</label></fieldset>}
function Field({title,children}:{title:string;children:React.ReactNode}){return <section className="sv2-field"><h2>{title}</h2>{children}</section>}
function SelectRow({checked,type,title,detail,onChange}:{checked:boolean;type:'radio'|'checkbox';title:string;detail:string;onChange:()=>void}){return <label className="sv2-row"><input type={type} checked={checked} onChange={onChange}/><span><strong>{title}</strong><small>{detail}</small></span></label>}
