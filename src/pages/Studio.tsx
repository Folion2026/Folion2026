import {BookOpen,Download,FileText,FolderKanban,RefreshCw,ShieldCheck,Sparkles} from 'lucide-react'
import {useMemo,useState} from 'react'
import {approvedDraftSections,FALLBACK_PROJECT_IMAGE,normalizeProjects,projectKnowledgeStatus,reviewedKnowledgeFacts} from '../lib/project'
import {useStore} from '../store'
import {Button,EmptyState} from '../components/ui'

type SourceType='collection'|'project'
const outputTypes=['Project Sheet','CV Package','Pitch Deck','Tender Response'] as const
type OutputType=typeof outputTypes[number]

export default function Studio(){
 const {collections,projects}=useStore();const safeProjects=normalizeProjects(projects);const safeCollections=(collections||[]).filter(Boolean);const [sourceType,setSourceType]=useState<SourceType>('project');const [sourceId,setSourceId]=useState(safeProjects[0]?.id||'');const [outputType,setOutputType]=useState<OutputType>('Project Sheet');const [generated,setGenerated]=useState(false);const [teamUse,setTeamUse]=useState<Record<OutputType,boolean>>({'Project Sheet':false,'CV Package':false,'Pitch Deck':false,'Tender Response':false})
 const sourceName=sourceType==='collection'?safeCollections.find(item=>item?.id===sourceId)?.name:safeProjects.find(item=>item.id===sourceId)?.projectName
 const items=useMemo(()=>sourceType==='collection'?safeProjects.filter(project=>(safeCollections.find(item=>item?.id===sourceId)?.projectIds||[]).includes(project.id)):safeProjects.filter(project=>project.id===sourceId),[sourceType,sourceId,projects,collections])
 const hasTeamInput=items.some(project=>Object.values(project.knowledge?.teamInput||{}).some(Boolean))
 const changeType=(type:SourceType)=>{setSourceType(type);setSourceId(type==='collection'?safeCollections[0]?.id||'':safeProjects[0]?.id||'');setGenerated(false)}
 const previewTitle=sourceType==='collection'?sourceName:(items[0]&&reviewedKnowledgeFacts(items[0]).find(fact=>fact.key==='projectName')?.value)||'Reviewed project knowledge'
 return <div><header className="studio-heading"><p className="eyebrow">Studio</p><h1 className="page-title mt-2">Turn reviewed knowledge into useful work.</h1><p>Studio uses only reviewed project facts, approved Folion Draft sections and Team input you explicitly allow for this output.</p></header><div className="studio-layout"><aside className="studio-controls"><div className="flex items-center gap-2 font-medium"><Sparkles size={18}/> New output</div><div className="source-type"><button onClick={()=>changeType('collection')} className={sourceType==='collection'?'active':''}><BookOpen/> Collection</button><button onClick={()=>changeType('project')} className={sourceType==='project'?'active':''}><FolderKanban/> Project</button></div><label className="label mt-6">Source<select value={sourceId} onChange={event=>{setSourceId(event.target.value);setGenerated(false)}}>{sourceType==='collection'?safeCollections.map(collection=><option key={collection.id} value={collection.id}>{collection.name||'Untitled collection'}</option>):safeProjects.filter(project=>project.status!=='Archived').map(project=><option key={project.id} value={project.id}>{project.projectName}</option>)}</select></label><label className="label mt-5">Output type<select value={outputType} onChange={event=>{setOutputType(event.target.value as OutputType);setGenerated(false)}}>{outputTypes.map(type=><option key={type}>{type}</option>)}</select></label><label className={`studio-team-use ${!hasTeamInput?'disabled':''}`}><input type="checkbox" checked={teamUse[outputType]} disabled={!hasTeamInput} onChange={event=>{setTeamUse(current=>({...current,[outputType]:event.target.checked}));setGenerated(false)}}/><span><strong>Allow Team input in this output</strong><small>{hasTeamInput?'Explicit approval applies only to this output.':'No Team input has been recorded for this source.'}</small></span></label><div className="source-summary"><span>{items.length} source {items.length===1?'project':'projects'}</span><strong>{sourceName||'Choose a source'}</strong>{items.map(project=><small key={project.id} className={projectKnowledgeStatus(project)==='Ready for Studio'?'ready':''}>{projectKnowledgeStatus(project)}</small>)}</div><Button className="w-full justify-center mt-5" disabled={!items.length} onClick={()=>setGenerated(true)}><Sparkles size={16}/> Generate preview</Button><p className="studio-note">Local prototype only. Unresolved facts and unapproved drafts are omitted automatically.</p></aside><section className="preview-page">{generated&&sourceName?<><div className="preview-toolbar"><div className="font-semibold text-xl">Folion <span>/ Studio</span></div><div className="flex gap-2"><Button variant="ghost" onClick={()=>setGenerated(false)}><RefreshCw size={15}/> Reset</Button><Button variant="ghost"><Download size={15}/> Export PDF</Button></div></div><div className="preview-cover"><p className="eyebrow">{outputType} · 2026</p><h2>{previewTitle}</h2><p>Prepared from the reviewed knowledge currently approved for reuse.</p><div><span>{items.length} project knowledge {items.length===1?'record':'records'}</span><span>Folion Studio</span></div></div>{items.map((project,index)=>{const facts=reviewedKnowledgeFacts(project);const factMap=new Map(facts.map(fact=>[fact.key,fact.value]));const drafts=approvedDraftSections(project);const teamValues=Object.values(project.knowledge?.teamInput||{}).filter(Boolean);const used=[facts.length&&'Reviewed project facts',drafts.length&&'Approved narrative',teamUse[outputType]&&teamValues.length&&'Team input'].filter(Boolean) as string[];return <article className="preview-project" key={project.id||index}><img src={project.coverImage||FALLBACK_PROJECT_IMAGE} onError={event=>{event.currentTarget.src=FALLBACK_PROJECT_IMAGE}} alt=""/><div><p className="eyebrow">0{index+1} · {projectKnowledgeStatus(project)}</p><h3>{factMap.get('projectName')||'Project knowledge under review'}</h3>{drafts.length?drafts.map(section=><p key={section.key}>{section.value}</p>):<p className="studio-omission">No approved narrative is available for this output yet.</p>}<div className="preview-facts">{['location','client','sector','status','year','siteArea','gfa','dwellings','height','services'].map(key=>factMap.get(key as never)).filter(Boolean).map(value=><span key={value}>{value}</span>)}</div>{teamUse[outputType]&&teamValues.length>0&&<div className="studio-team-extract"><strong>Approved Team input</strong>{teamValues.map(value=><p key={value}>{value}</p>)}</div>}<div className="used-knowledge"><ShieldCheck/><div><strong>Used knowledge</strong>{used.length?used.map(item=><span key={item}>{item}</span>):<span>No approved knowledge used</span>}</div></div></div></article>})}</>:<EmptyState title="Your preview will appear here" body="Choose a project or collection. Studio will include only knowledge that is approved for reuse." action={<FileText className="mx-auto text-black/30"/>}/>}</section></div></div>
}
